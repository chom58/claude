// 検索インデックス用のフック
const addSearchIndexHooks = (model, indexType) => {
  // 基本的な実装 - 必要に応じて後で拡張可能
  console.log(`Search index hooks added for ${indexType}`);
};

module.exports = { addSearchIndexHooks };
